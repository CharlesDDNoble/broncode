# mysite_uwsgi.ini file
[uwsgi]

plugin          = python3

# Django-related settings

# the base directory (full path)
chdir           = /home/charlesddnoble/senior_design/broncode/broncode

# Django's wsgi file
wsgi-file       = /home/charlesddnoble/senior_design/broncode/broncode/broncode/wsgi.py
# module        = broncode.wsgi:application

# the virtualenv (full path)
# home          = /path/to/virtualenv


# process-related settings
# master
master          = true

# maximum number of worker processes
processes       = 10

# the socket (use the full path to be safe
socket          = /home/charlesddnoble/senior_design/broncode/broncode/uwsgi_nginx.sock

# ... with appropriate permissions - may be needed
chmod-socket    = 664

# clear environment on exit
vacuum          = true